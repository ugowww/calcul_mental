import React, { Component } from 'react'
import NextButton from './index'

export default class Navigation extends Component {
  render() {
    return (
     <NextButton props={this.props}/>
    )
  }
}
